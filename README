Install:

brew install rbenv
rbenv init # make sure you .zshrc now ends with the output from this
open new terminal
rbenv install 2.1.2
which gem # make sure this is using rbenv
gem install bundler
rbenv rehash
bundle install


Run with:
rake
